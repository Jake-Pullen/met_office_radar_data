[project]
name = "met-office"
version = "1.0.0"
description = "Convert .dat nimrod files to .asc files"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3",
    "ruff>=0.14.3",
]
